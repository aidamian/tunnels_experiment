# 260320_210926 summary

## Result
- Overall status: PASS
- Scope: separate DinD runtime from host-side runtime and split the shell entrypoints by workflow

## Changes
- Replaced the shared `.runtime/tunnels.env` model with:
  - `.runtime/dind.env` for the DinD container and tunnel tokens
  - `.runtime/public_hosts.json` for host-side bridge consumers
- Updated `src/experiment_runner.py` and `src/utils/start_local_bridges.py` so the host-side Python path reads only public FQDNs from runtime files
- Added `src/utils/demo_config.py` for fixed non-secret host-side defaults
- Replaced `start.sh` with:
  - `start_e2e.sh`
  - `start_host.sh`
- Updated `docker-compose.yml`, `Makefile`, `README.md`, `IMPLEMENTATION.md`, and `AGENTS.md`

## Validation
- `python3 src/utils/prepare_runtime.py`
- `bash -n start_e2e.sh start_host.sh`
- `docker compose config -q`
- `python3 src/experiment_runner.py --help`
- `python3 src/utils/start_local_bridges.py --help`
- `python3 src/utils/wait_for_stack.py --help`
- `python3 src/utils/smoke_test.py --help`
- `python3 -m compileall src`
- `python3 -m py_compile $(find src -type f -name '*.py' | sort)`
- `./start_e2e.sh --duration-seconds 1`
- `timeout -s INT 90 ./start_host.sh --verify`

## Verified Runs
- Automated end-to-end run `260320_210435`: PASS
- Continuous host-testing run `260320_210613`: PASS
- Continuous run verification returned PostgreSQL `SELECT 1` -> `1`
- Continuous run verification returned Neo4j Bolt `RETURN 1` -> `1`
